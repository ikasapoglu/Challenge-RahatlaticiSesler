package com.challenge.rahatlaticisesler.data.remote

class ApiEndpoint {
    companion object {
        const val ENDPOINT_CATEGORIES = "categories.json"
        const val ENDPOINT_CATEGORY_DETAILS = "categories/{categoryId}.json"
    }
}